# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# OS
.DS_Store

# Local preview output (committed content/ is intentional, this is just scratch)
preview/
# Note: content/ and state/ ARE committed by the generate workflow on purpose.
# Instagram needs the rendered images to be publicly reachable, and the rolling
# history lives in state/history.json.

# Reddit review drafts and local engagement state stay on your machine. The
# drafts are for human review, and the state should not leak who was replied to.
state/reddit_queue/
state/reddit_state.json
